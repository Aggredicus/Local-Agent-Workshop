[project]
name = "local-agent-workshop"
version = "0.0.0"
description = "Local-first autonomous agent development workshop."
requires-python = ">=3.11"

[project.scripts]
workshop = "workshop.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
