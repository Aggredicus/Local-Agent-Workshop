# skill-security-auditor fixtures

This directory is reserved for `/skill-security-auditor` fixture inputs and expected reports.

The initial scaffold does not add executable scanner fixtures yet. Follow-up implementation should add:

```text
candidate-safe-skill/
candidate-review-needed-skill/
expected-safe-report.json
expected-review-needed-report.json
```

The first implementation issue should keep all fixture data synthetic and free of private values.
